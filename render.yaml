services:
  - type: web
    name: web-final-project-isrender
    env: node
    plan: free
    region: singapore
    buildCommand: npm install --build-from-source=sqlite3 && npm run build
    startCommand: npm start
